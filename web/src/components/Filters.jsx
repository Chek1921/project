import React from 'react'
export default function Filters({ userId, onUserId, day, onDay, onPrevious, onNext }) { return <div className="filters"><div className="daynav"><button type="button" aria-label="Предыдущий день" onClick={onPrevious}>‹</button><input aria-label="Дата отчёта" type="date" value={day} onChange={(event) => onDay(event.target.value)} /><button type="button" aria-label="Следующий день" onClick={onNext}>›</button></div><label className="analyst">Аналитик<input value={userId} onChange={(event) => onUserId(event.target.value)} /></label></div> }
